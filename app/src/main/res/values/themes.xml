<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CourseworkApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    
    <!-- Creating a subclass of Material3.Button and configuring my own attributes -->
    <style name="MyButtonStyle" parent="Widget.Material3.Button">
        <item name="android:textColor">@color/black</item>
        <item name="shapeAppearance">@style/MyButtonShape</item>
        <item name="backgroundTint">@color/lime_green</item>
    </style>

    <!--  This is necessary to make my buttons have no rounded corners  -->
    <style name="MyButtonShape" parent="ShapeAppearance.Material3.SmallComponent">
        <item name="cornerSize">0dp</item>
    </style>

    <style name="Theme.CourseworkApp" parent="Base.Theme.CourseworkApp" />
</resources>